apiVersion: batch/v1
kind: CronJob
metadata:
  name: openmetadata-snowflake-ingest
  namespace: daap-dev
spec:
  schedule: "*/15 * * * * "
  jobTemplate:
    spec:
      template:
        spec:
          containers:
            - name: metadata
              image: daappoc25.azurecr.io/openmetadata:custom
              command: ["metadata", "ingest", "-c", "/config/ingest.json"]
              volumeMounts:
                - name: ingest-config
                  mountPath: /config
          restartPolicy: Never
          volumes:
            - name: ingest-config
              configMap:
                name: openmetadata-snowflake-ingest-config